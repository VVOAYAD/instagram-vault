# Personal data — stays local only
Journal/
Downloads/
.obsidian/

# Pipeline state (local only)
.processed.json
.pending_plan.json
__pycache__/
*.pyc

# output/ must stay tracked — Instagram fetches slide images from GitHub CDN
